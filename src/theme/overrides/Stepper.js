// ----------------------------------------------------------------------

export default (theme) => ({
  MuiStepConnector: {
    styleOverrides: {
      line: {
        borderColor: theme.palette.divider,
      },
    },
  },
});
